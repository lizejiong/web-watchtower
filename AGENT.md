# AGENT.md

## 适用范围

当前仓库是一个 Web 监控平台，核心模块包括：

- `packages/sdk-web`：浏览器 SDK，负责采集、批量上报、离线重放
- `apps/ingest-api`：事件接收 API，负责鉴权、校验、幂等写入
- `apps/worker`：后台任务，负责 Issue 归并及后续异步处理
- `apps/dashboard`：管理后台
- `apps/playground-react`：本地联调和端到端验证入口

## 注释规则

- 注释统一使用中文，协议名、字段名、第三方产品名等专有名词可保留英文。
- 对外 API、导出类型、导出常量、配置项、跨包接口、协议 envelope/schema 必须使用 `/** */` 说明其用途和调用契约。
- 非导出的私有函数、局部变量和直白类型不强制逐一注释，优先通过清晰命名、类型约束和测试表达意图。
- 遇到复杂逻辑、协议边界、浏览器/Node 兼容分支、幂等/去重、采样、权限控制、重试、队列、隔离执行等代码时，必须补充解释 `why`、约束条件、失败模式或副作用。
- 注释应说明契约、边界、设计原因和非显然行为，不要复述代码正在做什么。
- 测试代码不强制注释；只有在模拟特殊运行环境、规避框架限制或断言不直观时才补充说明。
- 避免无信息量注释，例如“设置变量”“发送请求”“循环数组”。如果必须依赖注释才能读懂普通流程，优先改善命名或拆分函数。

## 代码组织

- 每个源文件都应具有清晰、明确的范围和目的。
- 当文件过大或处理过多关注点时，将其拆分。

## TypeScript

- 尽可能显式声明返回类型。
- 将复杂类型提取到专用类型type声明interface中。
- 优先复用 `packages/shared` 中的类型，不要在各应用中重复定义协议结构。
- 只有在第三方边界无法避免时才使用 `any`，且应限制影响范围。

## 测试（Vitest）

- 使用 `describe` / `it`，不要使用 `test`。
- 对复杂输出优先使用 `toMatchSnapshot`。
- 需要按特定语言或固定路径管理快照时，使用 `toMatchFileSnapshot` 并显式传入路径。
